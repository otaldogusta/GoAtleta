/// <reference types="expo/types" />

// Keep Expo's platform types available before `expo start` generates expo-env.d.ts.
// A fresh checkout must typecheck with the same declarations as local development.
